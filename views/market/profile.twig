<!DOCTYPE html>
<html lang="en">
<head>
    <title>Document</title>
    <link rel="stylesheet" href="../styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="body">

{% set currentPage = 'profile' %}
{% include 'Templates/navbar.twig' %}

<div class="container">
    <br>
    <h2 class="welcomeUser">Your profile</h2>
    <br>
</div>
<div class="container">
    <img src="avatars/{{ userInfo.avatar }}.png" class="border h-40 border-blue-500 rounded-full">
</div>

<br>

<div class="container">
    <div class="pt-2 mx-auto text-gray-600">
        <form method="GET" action="/UserSearch" id="my-form">
            <input class="border-2 border-gray-300 bg-white h-10 px-5 pr-150 rounded-lg text-sm focus:outline-none"
                   type="text" name="search" placeholder="Search user by email" id="my-search-field">
            <button type="submit" class="absolute right-0 top-0 mt-5 mr-4">
            </button>
        </form>
        <p class="error">{{ SearchError }}</p>
</div>
</div>

<br>
<br>



<div class="container">
    <form method="POST" action="/profile" class="mx-auto max-w-md">
        <label class="block font-bold mb-2 text-gray-700">
            Your name: {{ userInfo.name }}
        </label>
        <label class="block font-bold mb-2 text-gray-700">
            Current balance: {{ userInfo.balance | number_format(2, '.', ',') }} €
        </label>
        <label class="block font-bold mb-2 text-gray-700">
            Enter amount to increase or decrease balance:
        </label>
        <input type="number" step="0.01" name="amount" min="-999999" max="999999"
               class="border-2 border-gray-300 bg-white w-full py-2 px-3 text-gray-700 rounded-lg text-sm focus:outline-none">
        <button type="submit"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold mt-2 py-2 px-4 rounded focus:outline-none focus:shadow-outline">
            Update balance
        </button>
        <p class="error">{{ BalanceError }}</p>
    </form>
</div>


</body>
</html>