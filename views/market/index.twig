<!DOCTYPE html>
<html lang="en">
<head>
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="body">

{% set currentPage = 'market' %}
{% include 'Templates/navbar.twig' %}

<br>


<p class='welcomeUser'>Welcome, {{ userInfo.name }}!</p>
<p class='userBalance'>Your balance: {{ userInfo.balance | number_format(2, '.', ',') }} €</p>
{% include 'Templates/search.twig' %}

<div class="container">
<p class="error-xl">{{ TransactionError }}</p>
</div>

<div class="card-container">
    {% for cryptoCurrency in cryptoCurrencies %}
        {% set mySymbol = cryptoCurrency.symbol %}
            {% include 'Templates/card.twig' %}
    {% endfor %}
</div>

</body>
</html>